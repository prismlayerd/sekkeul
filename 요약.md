# 세끌 (Sekkeul) — 프로젝트 컨텍스트

> 마지막 업데이트: 2026-08-01
> 이 문서는 **지금 유효한 사실과 규칙**만 담는다. 무엇을 언제 고쳤는지는 git log에 있다.

## 앱 개요

한국 직장인·프리랜서·N잡러를 위한 세금·가계부 관리 앱.
Flutter 3 / Dart 3, Android·iOS·Web. **온디바이스 원칙**: 서버 없음, 모든 데이터는 기기에만.

**앱의 목적은 사전 처방이다.** 카드 지출 내역은 이미 국세청이 갖고 있다 — 그건 사후다.
"지출을 어떻게 해야 환급액을 키울 수 있느냐"를 미리 알려주는 것이 이 앱의 역할이다.
기준 귀속연도가 항상 **올해**여야 하는 이유도 이것이다.

- 패키지명 `secul`, applicationId `com.sekkeul.app`, version 1.0.0+4
- targetSdk/compileSdk 36, 릴리스 서명은 `android/key.properties` → `android/app/sekkeul-release.jks`
- 개인정보처리방침 `https://prismlayerd.github.io/sekkeul/` (문의 prismlayerd@gmail.com)
- 저장소 `github.com/prismlayerd/sekkeul`
- 스플래시 없음 — `main.dart`가 `HomeScreen()` 직행

### 대상 범위 — 개인 3유형만

**직장인 · N잡러 · 개인 프리랜서.** 사업자 기능은 만들지 않는다.
복식부기·성실사업자·법인은 범위 밖이고, 판정되면 계산을 멈추고 세무사를 권한다.

이 범위가 정해지자 죽은 분기가 드러나 삭제했다 — `isQualifiedFaithfulTaxpayer`는
켜는 UI가 없어 영구히 false였고, 그에 딸린 프리랜서 월세 세액공제 분기는
한 번도 발화한 적이 없었다. 표준세액공제도 언제나 7만원 하나다.

---

## 현재 상태

| 축 | 완성도 | 근거 |
|---|---|---|
| 핵심 기능 | **90%** | 화면 90개. 미구현 마커 사실상 없음(웹 플랫폼 제약뿐) |
| 계산 엔진 | **99%** | 조문 독립 검산 36인 1원 단위 일치, 경비율 표 1,542종 전수 대조, 근거 추적 19건 |
| 전달 층 | **96%** | 위젯 State에서 계산하는 37개 화면 **전부** 값 대조. 스모크 288회 무예외 |
| 검증 | **97%** | 변이 검출, 고시 만료 알람, 단일 출처 가드, 극단 입력 스윕 |
| 출시 준비 | **85%** | 비공개 테스트 진행 중, versionCode 4 대기, **실기기 0회** |

**종합: V1 출시 기준 95%**

### 지금 깨져 있는 것

없다. 테스트 412개 전부 통과.

**실기기 검증(0회)이 유일하게 남은 큰 구멍이다** — 코드로 못 덮는다.

### 근거가 약한 상수 1건

`test/notice_expiry_test.dart`가 고시에서 온 상수 19건을 추적하고, 그중 **1건이
1차 미확인**이라 매 실행 로그에 경고로 뜬다.

**경차 유류세 환급 — 부탄 리터당 197원.** 이건 원문을 못 찾은 게 아니라
**조문에 그 값이 없다** — 조특법 §111의2③은 휘발유·경유만 "리터당 250원"으로
정하고 부탄은 "부과된 개별소비세 전액"이라고만 한다. 부탄 개별소비세율에서
리터로 환산해야 하고 그 환산은 실무 관행이다. 조문이 값을 정하지 않는 한
이 항목은 계속 미확인으로 남는다 — 없앨 수 없는 종류의 불확실성이다.

### 비공개 테스트 (Google Play)

**Alpha 트랙 진행 중 — 테스터 12명 유지, 2026-08-01 기준 약 7일째.**
프로덕션 신청 가능 시점 **2026-08-08 전후**.

- **리셋 조건은 테스터 수다.** 하루라도 12명 밑으로 떨어지면 처음부터 다시.
- **새 버전 업로드는 카운트를 리셋하지 않는다.**
- versionCode 2 (DB v37) → **versionCode 3 (DB v42) 게시 완료** (2026-07-28 승인)
- 테스터 기기는 **v37 → v42를 한 번에** 지난다. 다섯 단계지만 전부 nullable 컬럼 ADD라 데이터 재작성이 없다
- 배포 크기 **24.8MB** (AAB 파일은 79.4MB지만 대부분이 디버그 심볼·proguard 맵이라 Play가 벗겨냄)
- 테스터 그룹 `sekkeul-testers@googlegroups.com`, 개발자 계정 `prismlayerd@gmail.com`

**스토어 등록정보에서 `가계부`를 뒤로 뺐다.** 열기도 전에 노동을 약속하는 말이고,
뱅크샐러드·편한가계부와 같은 칸에 놓이면 자동 연동 대 수기로 진다. 다만 검색어
가치가 있어 버리지 않고 제목 맨 뒤로 옮겼다.
현재 제목 `세끌 : 환급 계산기·절세·연말정산·종소세·가계부` (27/30).

**피처그래픽은 스크린샷으로 대체 불가.** Play가 이 자리를 크롭·리사이즈하고 위에
다른 요소를 겹치므로 UI·조밀한 글자를 넣지 말라는 게 가이드라인이다. 세로 9:16을
가로 1024×500에 넣는 것도 불가능하다. 생성 스크립트 `design/make_feature_graphic.py`.

### Play Console 함정

업로드에 실패한 항목이 App Bundle 칸에 빨간 표시로 남아 **"업로드 안 됨"처럼 보인다.**
실제 원인이 *"n 버전 코드는 이미 사용되었습니다"*라면 **첫 업로드는 성공한 것**이다.
실패 항목을 X로 지우고 **「라이브러리에서 추가」**로 이미 올라간 번들을 붙이면 된다.

### USE_EXACT_ALARM 정책

Play 정책상 알람시계·캘린더 앱 전용 → 제거하고 `SCHEDULE_EXACT_ALARM`만 유지.
Android 13+는 사용자가 직접 켜야 정시 발화, 안 켜면 `notification_helper.dart`가
비정확 알람으로 자동 폴백(크래시 없음, 시각만 지연).

---

## 디자인 시스템: AppTheme v4 "Blueprint"

| 토큰 | 값 |
|------|-----|
| 배경 (라이트) | Concrete `#F8F7F5` |
| 배경 (다크) | Matte `#0D0D0D` |
| 잉크 (라이트) | `#161513` / 부 `#5E5C57` / 라인 `#DCD8D0`·`#C7C2B8` |
| Accent | Blueprint Blue `#1F5AE0` |
| 서체 Display | DM Serif / Noto Serif KR |
| 서체 Body | DM Sans |
| 그림자 | **0** (없음) |
| Border-radius | ≤4px |

**하드 제약:** 바텀시트 금지 — fullscreen push / inline expand / AlertDialog만.
`ElevatedButton` / `OutlinedButton` 금지 — 기본 CTA는 `GestureDetector + Container`.

### 타입 스케일 — `app_theme.dart` 상수

| 상수 | px | 용도 |
|------|----|------|
| `tsXS` | 11 | 마이크로 라벨 / legend / eyebrow |
| `tsSM` | 12 | caption / meta / 부차 정보 |
| `tsMD` | 13 | compact UI / 탭 / 날짜 숫자 |
| `tsBase` | 14 | 기본 본문 / 목록 항목 |
| `tsLG` | 15 | 섹션 제목 / CTA |
| `tsXL` | 17 | sans UI 헤더 |
| `serifSM` | 17 | 인라인 serif / 다이얼로그 헤더 |
| `serifMD` | 22 | AppBar 제목 (전 화면 통일) |
| `serifLG` | 28 | 섹션 헤딩 / 빈 상태 제목 |
| `serifXL` | 34 | 표시 숫자 |

특수 예외(값 고정): `8.5` 달력 레인 / `18`,`22` 금액 입력 / `44` 홈 히어로 / `10` 하단 네비.
**이 외 크기는 사용하지 않는다.**

서체 3종은 `assets/fonts/` 번들(google_fonts 의존 제거) — 네트워크 없이 오프라인 렌더.

---

## 앱 구조 (4탭 IA)

```
홈 | 혜택 | 계산기 | 전체
```

상품 탭은 삭제됨. 금융상품 카드 기능은 **제외 확정(재제안 금지)**.

### 홈 (`home_screen.dart`)
- 상단 유형 탭(직장인/N잡러/프리랜서) — `_switchUserType` 즉시 전환. 유형별 가계부는 격리되지만 데이터는 안 사라짐(가져오기 배너)
- 회전 배너 — 카드별 X로 30일 숨김(`banner_states`)
- 현황 패널: 이달 수령액·지출 → 가계부 링크. 예상연봉·지출목표는 유형별 독립 저장(`profile_type_values`)
  - **프리랜서**: "예상 연봉" 없음. 헤드라인 "이번 달 수입 (세후)" — 탭하면 세전 환산 페이드
  - **N잡러**: 근로소득·다른소득을 각각 serif 44로 대등 표시. **합계는 표시 안 함**(성격이 달라 오해 소지). 근로소득은 간이세액표라 세전 역산 불가
  - 직장인 가계부의 올해 기타소득 소득금액이 300만원 초과 시 N잡러 전환 제안(연도별 1회)
- 카드공제 문턱 안내는 `_isEmployee` 분기(프리랜서는 카드 소득공제 대상 아님)
- 리마인더 패널, 세무도구 아코디언, 알림함 벨 배지

### 혜택 (`benefit_screen.dart`) — 9개 카테고리
정책·세무 혜택 카드. 전 항목 상세 + 공식 링크.

### 계산기 (`calculator_screen.dart`) — 10개 카테고리
급여·근로 / 세금·연금·환급 / 주거·부동산 / 대출·저축 / 부동산 세금 / 청년·장병 / 출산·육아 / 의료·복지 / 일자리·행정 / 교통·에너지.

재산세는 **물건별 개별 누진 후 합산**(합산 일괄계산 시 과다), 공정시장가액 60% + 도시지역분 0.14% 옵션.
종부세는 1주택 12억/부부공동 18억/2주택+ 9억, 주택수 무관 단일 누진, 1세대1주택 고령·장기보유 공제 최대 80%.

**정책금리는 범위 + 출처 + 확인일만 적는다.** 디딤돌·버팀목의 소득·만기별 세부표는
기금 공지로 수시 바뀌어 박아 두면 반드시 낡는다(종전 표는 2024년 값이라 1%p 가까이 낮았다).
계산은 어차피 사용자가 넣은 금리로 하므로 공시 범위만 남기고 기금e든든으로 보낸다.
디딤돌 연 2.85~4.15% / 버팀목 연 2.5~3.5% ([주택도시기금](https://nhuf.molit.go.kr) 2026-07-28 확인).

혜택·계산기 탭은 우상단 돋보기 → **제목 유지한 채 아래로 검색창 펼침**(`AnimatedSize`).

### 공제 고르기 (`deduction_gate_screen.dart`) — 계산기의 관문

입력 항목이 늘면서 계산기가 무거워지자 줄일 기준을 **측정**으로 정했다.

- **금액 기준으로는 못 나눈다.** 가장 작은 항목도 환급 7.5만원. 접을 축은 **해당자 비율**이다
- **세부 분해는 한도·고율에서만 갈린다.** 총급여 4,500만 기준으로 차이가 0원인 것:
  의료비 본인/65세↑, 교육비 초중고/대학, 연금저축/IRP 구분.
  갈리는 것: 난임 30만·교육비 300만 초과 90만·연금 600만 초과 45만

3단 구조:
1. **자동** — 총급여·카드사용·4대보험·부양가족은 프로필·가계부에서
2. **게이트** — 해당되는 것만 고른다. 체크리스트가 아니라 **가격표**다. 세법 용어 대신 "월세로 살아요"로 쓰고 오른쪽에 그 사람 총급여로 계산한 금액을 붙인다 (`deduction_options.dart`)
3. **되묻기** — 총액이 한도를 넘거나 고율 항목이 섞일 때만 세부 칸을 연다

숨긴 대가는 갚는다 — 안 고른 항목을 **주 CTA 앞에서** 금액과 함께 되묻는다.
선택은 `deduction_picks`(v41)에 저장돼 게이트를 안 거쳐도 적용된다.

### 내 정보 (`my_info_screen.dart`)
인라인 편집 6종(예상 연봉·**만 나이**·부양가족·자녀·거주 형태·급여일) + 「더 많은 공제 항목 입력하기」
→ `ProfileInputScreen`. **마법사는 이 6종을 다시 묻지 않는다** — 바로 위에서 답한 걸
또 묻고 있었다. 값은 프로필에서 로드·저장되므로 유지된다.

`나이`가 아니라 **`만 나이`**로 쓴다. 청년 감면(조특법 §30) 판정 기준이 만 나이라
세는나이로 넣으면 감면 판정이 틀어진다.

### 전체 / 설정
전체: 기록(가계부·리마인더·알림설정) / 세금(세무도구·양식) / 내 계정(내 정보·설정).
설정: 알림·화면 테마·데이터 수집방식·백업/복원·보안(앱 잠금)·오류 기록 내보내기·개인정보처리방침·면책·파기·버전.

**앱 내 피드백 통로는 없다.** 릴리스 빌드에 `INTERNET` 권한이 없어 전송이 실패하고
메일 주소만 복사되는 상태였다(2026-08-01 삭제). 다시 만들려면 권한 선언과 실기기
전송 확인이 먼저다 — 앱 안에 실패하는 버튼을 두는 것이 가장 나쁘다.
**부수 효과로 앱에 네트워크 호출이 하나도 없다** — 「완전 오프라인」이 문자 그대로 참이다.

---

## 세무 계산 엔진 (`lib/core/tax_engine/`)

**기준 귀속연도: 2026** (`tax_year.dart`의 `TaxYear.reference`).
회귀 테스트 `flutter test` **412개** 통과가 기준선.
(무거운 전수 스윕은 `test_slow/`에 있어 기본 실행에서 빠진다 — 20분 이상 걸린다.)

- `tax_rates.dart` — 세율 8구간·인적공제·자녀/출산·금융소득·원천징수율·국민연금 상하한·**최저임금**(화면 3곳이 참조)·경비율 적용식(단순/기준+소득상한배율)
- `employee_tax.dart` — 직장인 연말정산 전체 흐름. `calculateOtherIncomeAmount()`(기타소득 정률 40%)는 세 엔진 공유
- `freelancer_tax.dart` — 프리랜서 종소세(단순/기준경비율·실제 기장). 기타소득은 **별도 파라미터**로 받아 정률 40%만 과세(합쳐 넘기면 경비율 오적용). `compareBookkeepingVsEstimate()`. 표준세액공제 7만 고정
- `combined_tax.dart` — N잡러 합산(근로+사업+연금+기타). `actualExpense`를 받으므로 기장 vs 추계 비교도 프리랜서와 같이 된다
- `insurance_engine.dart` — 4대보험 **단일 출처**. `EmployeeTaxCalculator.calculateMonthlyInsurance`도 이걸 부른다(요율을 두 곳에 적으면 화면마다 다른 보험료를 말하게 된다)
- `reserve_estimator.dart` — 적립 카드용. `estimateForCurrentMonth()`가 저장 없이 매번 재계산
- `bookkeeping_duty.dart` — 기장의무 판정 + 무기장가산세
- `simple_ledger_builder.dart` — 가계부 → 국세청 간편장부 서식 + CSV(UTF-8 BOM)
- `occupation_data.dart` — 업종코드 1,542종 + 경비율
- `deduction_options.dart`(core/data) — 공제 항목 카탈로그
- `tax_year_rules.dart` — **경정청구 전용** 2021~2026 귀속별 규정 + 청구기한. 다른 화면은 쓰지 않는다(전부 올해 기준)

**기장의무 기준 (혼동 주의):**
- 직전연도 수입 3그룹: A(도소매·부동산매매·농림어·광업) **3억** / B(제조·숙박음식·건설·운수·정보통신·금융보험) **1.5억** / C(부동산임대·전문과학기술·교육·보건·예술·기타개인서비스 + 인적용역 940xxx) **7,500만**. 핵심 타깃 대다수가 C그룹
- 전문직은 수입 무관 무조건 복식부기
- **기장의무 그룹 임계 ≠ 경비율(단순/기준) 임계** — 다른 표
- **복식부기 자체 구현 안 함**(확정)

### 자녀세액공제 연령 (소법 §59의2① 개정, 법률 21548)

화면 문구는 **출생연도로 못박는다** — "9세 이상"은 만/연 나이 혼동을 부르고
2017년생 예외를 표현할 수도 없다. 5개 화면이 라벨 함수 하나(`childTaxCreditEligibilityLabel`)를 쓴다.

| 귀속연도 | 연령 | 대상 출생연도 |
|---|---|---|
| 2025 | 8세 이상 | 2017년생 이하 |
| **2026** | **9세 이상** | **2016년생 이하** |
| 2027~2029 | 10~12세 이상 | 2016년생 이하 |
| 2030~ | 13세 이상 | 2017년생 이하 |

2026~2029에 상한이 2016년생에 고정되는 건 **소득세법 부칙 §2③**이 2017년생을
경과규정에서 빼기 때문이다. 아동수당법 부칙 §2②와 짝을 이룬다.

---

## 세법 검증 체계

### 원칙 — 원문 없이 세법 상수를 고치지 않는다

**2차 자료(검색 요약)로 코드를 고쳐 두 번 틀렸다.** 둘 다 사용자가 "다시 확인해봐"라고
눌러서 잡혔다. 스스로 멈췄어야 했다. **조문·국세청 책자 원문을 열기 전에는 코드를 건드리지 않는다.**

원문 보관: `../sekkeul-지식/원문텍스트/` — 개정세법 해설 2022~2026년판 전문.
원본 PDF는 [국세청 개정세법해설 게시판](https://www.nts.go.kr/nts/na/ntt/selectNttList.do?mi=7133&bbsId=1083).
조문 대조는 국가법령정보센터(law.go.kr) 연혁법령.

### 검토 방향 — 코드 → 법

법 → 코드(개정 항목을 읽으며 "앱에 영향 있나?" 판단)는 하나를 빠뜨려도 티가 안 난다.
실제로 조특법 §126의2 자녀 카드한도를 통째로 놓쳤다.
**엔진 상수를 전부 나열하고 각각에 "이 값의 적용 귀속연도는?"을 묻는다.**

### 연 1회 발간물만으로는 부족하다

자녀세액공제 연령 개정은 2026.4.21.자인데 「2026년 개정세법 해설」은 2026.4.15. 발간이다.
6일 차이로 책자에 없다. **연 1회 갱신 때 law.go.kr 연혁·부칙을 함께 확인한다.**

### 연혁 전수 점검 기준선 (2026-07-27)

앱이 인용하는 법령 9건을 전수 확인했다. **다음 점검은 아래 번호 이후만 보면 된다.**

| 법령 | 확인 시점 최신 | 앱 영향 |
|---|---|---|
| 소득세법 | 법률 21548 (2026.4.21) | **있음 — 자녀세액공제 연령(수정 완료)** |
| 소득세법 시행령 | 대통령령 36343 (2026.5.22) | 없음 |
| 조세특례제한법 | 법률 21796 (2026.6.16) | 없음 |
| 조특법 시행령 | 대통령령 36423 (2026.6.23) | 없음 |
| 국세기본법 | 법률 21712 (2026.6.2) | 없음 (§45의2 경정청구 불변) |
| 국민연금법 | 법률 21689 (2026.5.26) | 없음 (요율 불변) |
| 국민건강보험법 | 법률 21687 (2026.5.26) | 없음 (요율·소득월액 불변) |
| 지방세법 | 2026.4.15 이후 개정 없음 | 없음 |
| 아동수당법 | 법률 21489 (2026.3.20) | 혜택 카드 문구 갱신 |

**⚠ "현행" 헤더를 믿지 말 것.** law.go.kr이 기본 표시하는 `[시행 …] [법률 …]`은
**시행일 기준**이라 그 뒤에 공포된 개정이 안 보인다. 조특법은 헤더가 "2025.12.23"인데
실제로는 4·5·6월 개정이 네 건 더 있었다. 반드시 **연혁 목록(`lsRvsRsnListP.do`)**으로 공포일을 봐야 한다.

### 4대보험 요율 원문 대조 기준선 (2026-07-27)

| 값 | 근거 |
|---|---|
| 국민연금 사업장 본인 4.75% / 지역 9.5% | 국민연금법 §88③④ + **부칙 <법률 제20903호> §4** (2026~2032 단계 인상표) |
| 건강보험료율 7.19% | 국민건강보험법 시행령 §44① |
| 재산보험료부과점수당 211.5원 | 같은 영 §44② |
| 월별 보험료액 상한 918만 3,480 / 459만 1,740, 하한 2만 160 | **보건복지부고시 제2025-222호** |
| 장기요양 0.9448% | 노인장기요양보험법 시행령 §4 |
| 고용보험 근로자 0.9% | 보험료징수법 시행령 §12①2 |
| 특고 고용보험 0.8% | 같은 영 §56의7④ |
| 노무제공자 산재요율 (본인 0.25~0.9%) | **고용노동부고시 제2025-92호** ÷2 |

고시 유효기간이 2026.12.31.로 끝나므로 **매년 갱신 대상**이다.

**구조 규칙:** 건강보험 고시가 정하는 것은 '소득'이 아니라 **보험료액**이다.
소득에 캡을 걸고 요율을 곱하면 하한이 어긋난다.

### 적용시기 문구를 그대로 읽는다 — "연도판 = 귀속연도"가 아니다

2023년판 월세 공제율 상향은 *"2023.1.1. 이후 **신고하거나 연말정산하는 분**부터"* —
2023년 1~2월 연말정산은 2022년 귀속이라 15/17%는 **2022 귀속부터** 적용된다.
반면 2024년판 월세 한도 상향은 *"개시하는 과세연도분부터"* — 이건 귀속연도 기준이다.

### 기한도 조문으로 — 경정청구는 5월 31일이 아니다

국세기본법 §45의2 **⑤항**이 연말정산 대상자에게는 "법정신고기한"을 **"연말정산세액의
납부기한"**으로 바꿔 읽게 한다. 연말정산은 다음 해 2월분 급여에서 원천징수하고(소법 §137①),
원천징수세액은 다음 달 10일까지 납부한다(§128①).
→ 기한은 **귀속연도 다음 해 3.10. + 5년**. 2021 귀속은 2027.3.10. 마감.

### 값의 정확성은 테스트가, 적용연도는 고시 만료 알람이 지킨다

값이 틀리면 테스트가 깨진다. 값은 맞고 **연도만 틀린 오류**는 테스트로 안 잡히므로,
`test/notice_expiry_test.dart`가 고시에서 온 상수 11건에 **출처 + 유효기간 + 확인일**을
붙여 두고 기간이 지나면 깨진다(30일 전부터 경고). 깨지면 할 일은 하나다 —
`source`의 원문을 열어 새 값을 확인하고 상수·`until`·`checkedOn`을 함께 고친다.
**검색 요약으로 고치지 않는다.**

추적 중 19건: 국민연금 상·하한 · 건보요율·부과점수당·보험료액 상하한 · 장기요양요율
· 특고 고용보험료율 · 최저임금 · 구직급여 상·하한 · 근로장려금 상한·산정식
· 자동차세 연납 공제율 · 기초연금 선정기준액 · 본인부담상한액 · 경차 유류환급
· 주택연금 월지급금 · 경비율 고시 귀속연도.

`primaryVerified: false`는 **값이 틀렸다는 뜻이 아니라 근거가 약하다는 표시**다.
매 실행 로그에 남아 "확인했다고 착각한 채 넘어가는 것"만 막는다.

### 조문 독립 검산 — 엔진을 빌려 쓰지 않는다

`test/support/tax_law_reference.dart`는 **엔진을 import하지 않는다.** 상수를 빌려 쓰면
상수가 틀렸을 때 양쪽이 똑같이 틀려 대조가 통과해 버린다. 세율 8구간·근로소득공제·
근로세액공제 한도식·4대보험 요율·카드공제·의료비·교육비·기부금을 조문에서 직접 옮겨 적었다.

3유형 36인(직장인 12·프리랜서 12·N잡러 12)이 각각 내 정보 + 3개월 가계부를 갖고
**1원 단위로 일치**한다. 경비율 표 1,542종은 국세청 고시 원문 스냅샷
(`test/fixtures/expense_rate_notice_2025.json`)과 전수 대조한다.

### 테스트가 일하는지는 변이로 확인한다

통과만으로는 아무것도 증명되지 않는다. 세율·한도·공제율·상수에 의도적 오류를 넣고
잡히는지 본다 — **26건 중 26건 검출**(2026-08-01).

한 번은 놓쳤다. 홈 진행바 넘침을 되살렸는데 전수검사가 통과했다 — 시드가 카드 문턱을
이미 넘겨 **문제의 진행바가 아예 안 그려지고 있었다.** 문턱 미달 시드를 따로 넣어 막았다.
전수검사는 "훑었다"가 아니라 **"그 코드가 실제로 그려졌나"**를 봐야 한다.

### 법정 상수는 엔진 한 곳에만 둔다

`test/single_source_of_law_test.dart`가 **소스를 훑어** 화면이 §55① 세율 구간표를
복사했는지, 매년 바뀌는 요율·최저임금을 직접 적었는지 본다.

값이 틀렸는지는 조문 대조가 잡지만, 이 병은 **오늘 맞아도 내년에 틀어진다.**
실제로 양도세 세율표·홈 한계세율이 2022년 이전 값으로 굳어 있었고, 4대보험이
두 벌, 최저임금이 세 화면에 각각 박혀 있었다.

### 자릿수를 틀린 입력에 조 단위로 답하지 않는다

`test_slow/extreme_input_sweep_test.dart`가 64개 화면을 열어 앞 4칸에 큰 수를
넣고 1조원 넘는 금액이 그려지는지 본다. 9개 화면이 폭발했고(최대 1.5e22원,
int64 최대값까지) 지금은 0건이다.

`saneInput`·`InputMax`(`amount_field.dart`)로 **칸의 의미별** 상한을 씌운다 —
금액 상한 하나로는 못 막는다. 금리 칸에 8자리를 넣는 게 문제였고, 차량가격 칸은
만원 단위라 원 단위 상한을 쓰면 1만 배로 부푼다.

⚠ 20분 이상 걸려 `test/`가 아니라 `test_slow/`에 있다. 계산기를 건드렸으면
`flutter test test_slow/`로 직접 돌린다.

### 페르소나 회귀 (34인)

`dbService`를 `InMemoryDatabaseHelper`로 갈아끼우고 실제 화면이 부르는 함수를 그대로 호출한다.
코드만 읽어선 안 잡히는 **배선 버그**를 잡는다.
- `test/employee_persona_test.dart` — 직장인 12인
- `test/freelancer_persona_test.dart` — 프리랜서 10인
- `test/njob_employee_persona_test.dart` — N잡러 10인 + 직장인 8인
- `test/bracket_boundary_test.dart` — 소득 구간 경계 1원 단위 29건

**페르소나가 통과해도 화면이 틀릴 수 있다.** 페르소나는 엔진을 부르고, 사용자는 화면을 쓴다.

### 화면 검증 — 빈 프로필로만 열면 아무것도 못 잡는다

「프로필을 저장한 사용자 전원이 겪는 크래시」가 297개 테스트를 통과한 채 알파 트랙까지
나갔다. `getProfile()`이 bool로 정규화해 돌려주는 값을 `as int?`로 캐스팅한 코드가
있었는데, **빈 프로필에서는 null이라 무사통과**했다. 실제 사용자만 bool을 갖는다.

- `screen_smoke_filled_profile_test` — **채워진 프로필 + 3개월 가계부**로 240개,
  빈 프로필로 48개 화면을 열고 **예외를 전부 실패로 본다**. 넘침 검사는 화면이 못 뜨는
  걸 일부러 삼키므로(관심사가 레이아웃) 그쪽으로는 안 잡힌다.
- `profile_type_contract_test` — `getProfile()` 반환 타입 계약(불리언 16·카운트 7)을
  못박고, 소스를 훑어 잘못된 캐스팅이 다시 생기는 것도 막는다.
- `screen_value_crosscheck` / `tax_simulator_value_test` / `calculator_law_crosscheck` /
  `property_tax_law_crosscheck` — 위젯 트리의 `Text`를 긁어 **그려진 숫자**를 조문 검산과
  대조한다. 계산이 위젯 State 안에 있는 계산기들은 이 방법 말고는 검증할 길이 없다.

**위젯 테스트에서 같은 화면을 두 번 펌프할 때는 루트 키를 바꾼다.** 안 그러면 State가
재사용돼 `initState`가 다시 안 돌고, 앞 화면 상태로 검사한 셈이 된다.

---

## 세무도구 파이프라인

3화면: ①진단(`tax_simulator_screen.dart`) → ②가상신고서 → ③홈택스 가이드(`tax_annual_report_screen.dart`).
상단 `TaxPipelineRail(current:)` 공통.

- **①진단**: 업종코드 → 기장의무 판정 → 수입 확정(PDF 파싱 or 직접 입력) → 경비(가계부 연동) → 기장 vs 추계 비교. 복식부기 판정 시 계산 숨김+세무사 권장
- **②가상신고서**: ①결과 자동 채움(`report_drafts`)
- **③홈택스 가이드**: 직장인=연말정산 재계산 / 프리랜서·N잡러=국세청 전자신고 매뉴얼 기반 6단계
- **장부 만들기**: 기장의무 판정 + 무기장가산세 20% 안내 + 간편장부 CSV export
- **메뉴 구성**: `taxRecordEntryFor()` — 프리랜서만 null. 경정청구는 직장인·N잡러만
- 업종코드 영속: `profile.occupation_code` 단일 출처

**남은 도메인 과제**: 74/82호 공식 서식 PDF 번들(국세청 수급 필요).

---

## 알림 시스템 — 리마인더 vs 알림 (분리 원칙)

- **리마인더**(개인): `kind='custom'` 자유 편집 / `kind='record'`(월급날+기록 넛지 18시) 시드 1회·시각만 편집
- **이벤트형 기본 리마인더**(`event_reminder_prefs`, v28) 5종 — `budget_alert`, `inactivity_nudge`, `income_inactivity_nudge`, `recurring_expense_alert`, `tax_reserve_shortfall`. 조건 해소 시 자동 취소
- **알림 히스토리 백필**(v27): OS 발화 알림을 앱 실행 시 체크포인트 이후 역산(최대 30일)
- **알림**(전체 브로드캐스트): `system_reminder_catalog.dart` 정적 정의. notifId 대역 1001~1099 세금 / 1100 교통·에너지 / 1200 일자리·행정 / 2000 개인·월급·카드
- **원칙**: 개인 상황 의존 혜택·특정 연도 한정 이슈는 브로드캐스트 금지
- **미구현 갭 4종**: 프리랜서용 기록 넛지 문구, 5월 원천징수 정산 안내, 지급명세서 확인, 건보 지역가입자 전환 안내

### 문구 길이 상한 — 제목 16자 · 본문 30자

안드로이드 접힌 알림은 제목·본문을 **각각 한 줄만** 보여주고 나머지를 `…`로 자른다.
`test/notification_length_test.dart`가 상한을 못박고, **본문이 제목을 반복하는 것도 막는다**.
`runes`로 세어 이모지가 섞여도 사람이 보는 글자 수와 맞다.

**제목 줄을 브랜드명에 쓰지 않는다.** 안드로이드는 이미 앱 이름을 아이콘 옆에 보여주므로
`세끌 리마인더`는 두 번 낭비다. 제목에 내용을 올리고 본문은 비운다.

**설명 대신 누를 이유를 남긴다.**
"오늘부터 종합소득세 신고예요. 환급 대상인지 미리 확인해보세요." → **"나는 환급일까, 납부일까?"**

---

## 가계부 (`expense_calendar_screen.dart` + `day_entry_screen.dart`)

- **레이아웃**: 달력 그리드 → 뷰 전환 탭(달력/분석/연간) → 적립 예상 카드(프리랜서·N잡러) → 퀵설정 칩 행 + 범례
- **적립 카드**: `ReserveEstimator` 매번 재계산. min≠max면 "단순경비율(최소)~기준경비율(최대)" 근거 1줄. 업종코드 미설정 시 숫자 숨김
- **일별 입력**: 날짜 탭/드래그 다중선택 → 풀스크린 `DayEntryScreen`. 수익 칩은 `LedgerProfile.of(userType).incomeTypes` 기준. 원천징수 토글은 `tracksBusinessExpense` 유형만. 지출 3행 각각 "사업경비로 인정" 체크박스
- **유형 이동(가져오기)**: 4방향만 — 직장인·프리랜서→N잡러(전 소득), N잡러→직장인·프리랜서(대상 지원 소득만). `movableIncomeTypesTo(target)`가 급여→프리랜서·사업소득→직장인 유출을 원천 차단
- **퀵입력 프리셋**(v33), **핀치 줌**(1단계 7열 / 2단계 가로 2배+관성)

---

## DB 버전 이력 (현재 v42)

| 버전 | 변경 내용 |
|------|-----------|
| v1~30 | 기본 스키마 · 알림 · 정기결제 · 카드결제일 · 이벤트 리마인더 · 차량/주택 보유 · 유형별 예상연봉 |
| v31 | expense_items.is_business, income_entries.is_withheld, recurring_templates.is_business |
| v32 | occupation_code, property_value, 4대보험 가입여부 4종 |
| v33 | quick_entry_presets |
| v34 | prior_year_income, is_new_business, has_multiple_businesses |
| v35 | expenses·income_entries에 user_type (조회는 `user_type = ? OR user_type IS NULL`) |
| v36 | error_log (최근 100건 순환) |
| v37 | monthly_income_records DROP |
| v38 | freelancer_health_insurance — 기능 제거됨. 컬럼만 잔존, 읽는 코드 없음 |
| v39 | children_count_total (카드공제 자녀 한도용) |
| v40 | children_count_credit (자녀세액공제 연령 개정으로 정의가 바뀌어 종전 값 미승계) |
| v41 | deduction_picks |
| v42 | newborn_year — 출산·입양 **귀속연도**. 수만 저장하면 내년에도 남아 없는 공제를 넣게 된다 |

**마이그레이션은 전부 nullable 컬럼 ADD + try/catch.** DROP·데이터 재작성 없음.

---

## 핵심 데이터 모델

### ExpenseItem
```dart
id, date, endDate?,   // endDate 있으면 범위 지출
amount, content,
category,             // 지출 카테고리 (식비/교통/의류 등 10종)
paymentMethod,        // 신용카드 | 체크+현금 | 기타
isBusiness,           // (v31) 사업경비 인정 여부 — category와 독립 플래그
userType?             // (v35) NULL=공통(구 데이터), 값 있으면 해당 유형 전용
```

### IncomeEntry
```dart
id, date, amount, incomeType,  // '급여' | '기타소득' | '사업소득' | '기타'(레거시)
isWithheld,                    // true면 amount가 원천징수 후 순액 — 세전은 저장 안 하고
                               // 표시 시점 역산(사업 ÷0.967, 기타 ÷0.912)
userType?                      // (v35)
```
프리랜서 `isWithheld` 기본 true, 직장인·N잡러 기본 false.
근로소득('급여')은 간이세액표라 역산 불가 — 세전/세후 전환 UI 없음.

### RecurringTemplate / RecurringConfirmation (v25)
```dart
// Template: id, name, amountHint, category, paymentMethod, isBusiness, dayOfMonth(1~31), sortOrder
// Confirmation: (templateId, year, month) PK, status: 0=대기/1=확인/2=건너뜀, actualAmount, expenseId
```

---

## 기술 패턴 · 함정

### kIsWeb 가드
```dart
if (kIsWeb) { /* InMemoryDatabaseHelper — 새로고침마다 전체 리셋 */ }
else        { /* SqfliteDatabaseHelper */ }
```
저장 지속성·마이그레이션은 **실기기에서만 검증 가능**.
웹 프리뷰 확인 전에는 `flutter build web`을 반드시 재실행(APK만 빌드하면 프리뷰가 묵음).

### DB 마이그레이션
```dart
if (oldVersion < N) { try { await db.execute('ALTER TABLE ...'); } catch (e) {} }
// 항상 오름차순
```

### flutter_local_notifications v22
리시버 자동병합 안 됨 → `AndroidManifest.xml`에 `ScheduledNotificationReceiver`/`BootReceiver` 직접 선언 필수.

### Flutter web `bottomNavigationBar` 렌더링 함정
`Row(children:[Expanded(...)])`를 높이 제약 없이 `bottomNavigationBar`에 직접 주면 body·appBar까지
통째로 안 그려짐. **동시에** 안드로이드에서는 `SafeArea`를 고정 높이 `SizedBox`로 감싸면
시스템 내비 인셋이 흡수돼 버튼이 가려짐. 정답 패턴:
```dart
bottomNavigationBar: SafeArea(top: false,          // 인셋은 바깥에서, 높이 제약 없음
  child: Container(padding: ...,
    child: SizedBox(height: 48,                    // 웹 렌더링용 높이는 안쪽 Row에만
      child: Row(children: [Expanded(...)]))))
```

### Flutter web은 캔버스 렌더
접근성 트리가 비어 브라우저 `read_page`/`form_input`이 안 먹는다 — 스크린샷 좌표로 조작해야 함.

### `tax_simulator_screen.dart` 중첩 함정
`build()`의 최상위 `if (_isFreelancer)`가 대부분을 감싼다. 새 카드를 그 안에 넣으면 직장인에게 안 보인다.
같은 입력이 **직장인 경로·프리랜서 경로 양쪽에** 있는 경우가 많다 — 한쪽만 고치면 절반만 고친 것이다.

### 한글 줄바꿈 `keepWords`와 이모지
어절 사이에 word-joiner(U+2060)를 넣는 확장인데, UTF-16 코드유닛으로 자르면 이모지의
서로게이트 쌍이 반으로 갈려 렌더가 통째로 죽는다. **문자열을 인덱스로 자를 땐 항상 `runes`.**

### 프로필 로드와 계산 순서
`initState`에서 프로필을 비동기로 읽는 동안 계산이 먼저 끝나면 빈 값으로 계산된 결과가
그대로 남는다(환급 과대 방향). 로드 완료 후 반드시 재계산한다.

### 데이터 저장 보안
`sqflite_sqlcipher`로 DB 파일 자체 암호화. 키는 `flutter_secure_storage`(Android Keystore/iOS Keychain)에
기기 내부 생성·보관, 서버 전송 없음(`db_key_manager.dart`). 기존 평문 설치는 `initDatabase()`에서
1회 자동 이전(임시파일 검증 후 원자적 교체, 실패 시 원본 보존). 백업 JSON `formatVersion: 2`.

---

## 앞으로 할 일

### 1. 다음 배포 (versionCode 4)
프로필을 저장한 사용자 **전원**이 겪던 크래시 2건(서류 체크리스트·혜택 탭)과
취득세 지방교육세 2배가 versionCode 3에 들어 있다. 알파 트랙 업로드는 테스터
카운트를 리셋하지 않으므로 프로덕션 신청 일정에 영향 없다.

### 2. 실기기 검증 (사용자 수행, 출시 전 필수)
웹으로 재현 불가한 것들. **코드로 덮을 수 없는 유일한 영역**이다.
1. versionCode 4 업데이트를 받은 뒤 **가계부 기록이 그대로인지** — v37→v42 마이그레이션이 실기기에서 도는 첫 순간
2. 유형 전환 후에도 "공통" 기록이 계속 보이는지
3. 앱 재실행 → 정상 구동되면 SQLCipher 자동 이전 완료
4. 생체 인증·PIN 폴백·백그라운드 복귀 잠금
5. 비행기 모드에서 폰트 3종 정상 렌더
6. 알림 문구가 실제로 안 잘리는지(제목 16/본문 30 기준이 이 기기에서 맞는지)

### 3. 비공개 테스트 완주
- 12명 × 14일 유지 → **2026-08-08 전후** 프로덕션 신청
- 12명 밑으로 하루라도 떨어지면 리셋

### 4. 남은 검증 구멍
- **연말정산 화면(`year_end_tax_screen`)**은 사용자가 직접 타이핑해야 값이 나와 아직 값 대조가 없다
- **문구는 안 본다.** 값 대조는 금액이 화면에 있는지만 확인하므로 라벨이 잘못 붙어도 통과한다
  (취득세 라벨 오류는 테스트가 아니라 코드를 읽어서 잡았다). 다크모드·접근성도 범위 밖
- 취득세 **농어촌특별세**를 주택수·가액으로 근사한다(조문은 전용면적 85㎡ 초과 기준)
- 종부세 **3주택 이상 중과세율**(2.0~5.0%) 미반영 — 단일 누진만 쓴다. 개인 3유형 범위에서는 수용 가능하나 다주택 고가에서 과소 계산된다
- 주택연금은 **억당 월지급금 선형보간**이라 HF 공시 예시와 소수점 단위 차이가 남는다(공시값 자체는 반영 완료)

### 5. 자산
- **아이콘 재제작** — 라이트모드 안 2종(`design/make_icon_light.py`, 면/선)이 대기 중. 현재는 구 다크 아이콘
- 아이콘 확정 후 피처그래픽도 같은 마크로 재생성

### 6. iOS
- 실기기 검증 트랙 별도 진행

### 7. 사용자 직접 작업
1. syncfusion_flutter_pdf Community License 조건(연매출 $1M 미만) 확인
2. 구 저장소(`vaekwoshi/connect`) 삭제

---

## 사업 전략

자체성장 + 반매각 옵션. **B2B 성과수수료 전용**(앱 내 IAP 없음).
2모드 구조 — 1모드: 온디바이스 매칭 / 2모드: 리드 유출. **2모드 착수 전 중개업 법률 게이트 필수.**
무자본 마케팅: 결과공유카드 + 커뮤니티 시딩 + 예비창업패키지.
