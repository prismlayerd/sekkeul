# 세끌 (Sekkeul) — 프로젝트 컨텍스트

> 마지막 업데이트: 2026-07-26 (세법 전수 대조 3라운드 완료 — 확정 오류 12건 수정, 기준 귀속연도 2026 상향)

## 앱 개요

한국 직장인·프리랜서·N잡러를 위한 세금·가계부 관리 앱.
Flutter 3 / Dart 3, Android·iOS·Web 지원. **온디바이스 원칙**: 서버 없음, 모든 데이터는 기기에만 저장.

**앱의 목적은 사전 처방이다.** 카드 지출 내역은 이미 국세청이 갖고 있다 — 그건 사후다.
"지출을 어떻게 해야 환급액을 키울 수 있느냐"를 미리 알려주는 것이 이 앱의 역할이다.
기준 귀속연도가 항상 **올해**여야 하는 이유도 이것이다(지난해 규정으로 올해 소비를 안내하면 안 됨).

- 패키지명 `secul`, applicationId `com.sekkeul.app`, version 1.0.0+2
- targetSdk/compileSdk 36, 릴리스 서명은 `android/key.properties`에서 로드(없으면 debug 폴백)
- 개인정보처리방침: `https://prismlayerd.github.io/sekkeul/` 호스팅 완료 (문의 prismlayerd@gmail.com)
- 스플래시 없음 — `main.dart`가 `HomeScreen()` 직행. 런처 아이콘 `design/icon_final.png`

---

## 현재 완성도 (2026-07-26 평가)

| 축 | 완성도 | 근거 |
|---|---|---|
| 핵심 기능 구현 | **90%** | 화면 90개. 코드에 미구현 마커 사실상 없음(웹 플랫폼 제약뿐) |
| 계산 엔진 정확도 | **85%** | 3라운드 대조 12건 수정, 페르소나 34인, 테스트 168개 |
| 검증 | **70%** | 엔진·렌더링 확인. **실기기 미검증**이 최대 리스크 |
| 출시 준비 | **75%** | Play 비공개 테스트 검토 제출 완료, 스토어 자산 전부 완료 |

**종합: V1 출시 기준 75% · 제품 기능 88%**

### 가장 급한 것 — 경정청구가 틀린 값을 보여준다

과거 5년치를 **2026 규정으로** 계산한다. 미구현보다 나쁜 상태다 — 화면에 금액이 나오는데
2021~2023년이면 연금저축 한도·월세 공제율이 그때와 다르다. 사용자가 그 숫자로 홈택스에 신고하면 문제가 된다.

막힌 지점은 자료가 아니라 **파서다**. `WithholdingReceipt`에 귀속연도 필드가 아예 없어
영수증이 몇 년치인지 모른다. 순서: ①파서 귀속연도 추출 → ②연도별 공제율 표 → ③분기.

필요 자료: 국세청 「원천징수의무자를 위한 연말정산 신고안내」 **2021~2024 귀속 4권**
(2025는 `지식_변환/JSON/2025_연말정산_공제율_정답지.json`로 이미 있음).
개정세법 해설은 델타만 보여줘서 부적합 — 5권을 역으로 체이닝해야 하고 오차가 누적된다.
경정청구가 쓰는 값은 6항목(의료비·교육비·기부금·보장성보험·연금저축·월세) × 5년 = 30개뿐.

---

## 출시 진행 상황 (Google Play)

**현재: 비공개 테스트(Alpha) Google 검토 제출 완료 — 검토 대기 중.**

### 완료
- **스토어 등록정보**: 앱 이름·설명(톤앤매너 편집본, "잘 쓰는 것도 환급" + 사전처방 차별화 + 온디바이스)·아이콘 512·피처그래픽 1024×500·스크린샷 5장
- **앱 설정 체크리스트 11종 + 광고 ID 선언**: 콘텐츠 등급(전체이용가)·타겟층(만 18세 이상)·개인정보처리방침·광고 없음·데이터 보안·정부/금융/건강 선언·카테고리(금융)
- **Alpha 트랙**: AAB(versionCode 2) 업로드 + 대한민국 + 테스터 그룹 `sekkeul-testers@googlegroups.com`

### USE_EXACT_ALARM 정책 대응
Play 정책상 `USE_EXACT_ALARM`은 알람시계·캘린더 앱 전용 → 제거하고 `SCHEDULE_EXACT_ALARM`만 유지.
Android 13+는 사용자가 "정확한 알람"을 직접 켜야 정시 발화, 안 켜면 `notification_helper.dart`가
비정확 알람으로 자동 폴백(크래시 없음, 시각만 지연). 알림 켤 때 `_requestExactAlarmPermission()`이
안내 다이얼로그 후 시스템 설정 유도.

### 남은 일
1. Google 검토 승인 대기 → 승인 후 옵트인 링크 생성
2. **테스터 12명 확보** — 개발자 품앗이. 가입 링크 `https://groups.google.com/g/sekkeul-testers`. 봇 대행(apptesters.cc 등)은 계정 정지 리스크로 사용 안 함
3. 12명 × 14일 연속 활동 충족 시 프로덕션 신청 가능
4. iOS는 실기기 검증 트랙 별도 진행

---

## 디자인 시스템: AppTheme v4 "Blueprint"

| 토큰 | 값 |
|------|-----|
| 배경 (라이트) | Concrete `#F8F7F5` |
| 배경 (다크) | Matte `#0D0D0D` |
| Accent | Blueprint Blue `#1F5AE0` |
| 서체 Display | DM Serif / Noto Serif KR |
| 서체 Body | DM Sans |
| 그림자 | **0** (없음) |
| Border-radius | ≤4px |

**하드 제약:** 바텀시트 금지 — fullscreen push / inline expand / AlertDialog만 허용.
`ElevatedButton` / `OutlinedButton` 금지 — 기본 CTA는 `GestureDetector + Container`.

### 타입 스케일 — `app_theme.dart` 상수

| 상수 | px | 용도 |
|------|----|------|
| `tsXS` | 11 | 마이크로 라벨 / legend / eyebrow |
| `tsSM` | 12 | caption / meta / 부차 정보 |
| `tsMD` | 13 | compact UI / 탭 / 날짜 숫자 |
| `tsBase` | 14 | 기본 본문 / 목록 항목 |
| `tsLG` | 15 | 섹션 제목 / CTA |
| `tsXL` | 17 | sans UI 헤더 |
| `serifSM` | 17 | 인라인 serif / 다이얼로그 헤더 |
| `serifMD` | 22 | AppBar 제목 (전 화면 통일) |
| `serifLG` | 28 | 섹션 헤딩 / 빈 상태 제목 |
| `serifXL` | 34 | 표시 숫자 |

특수 예외(값 고정): `8.5` 달력 레인 / `18`,`22` 금액 입력 / `44` 홈 히어로 / `10` 하단 네비.
**이 외 크기는 사용하지 않는다.**

서체 3종은 `assets/fonts/` 번들(google_fonts 의존 제거) — 네트워크 없이 오프라인 렌더.

**최종 디자인 대수술**(아이콘 재제작 + IA·메뉴·기능배치 전면 재설계)은 **맨 마지막 단계** 원칙.
"Blueprint, Evolved" 방향 Figma·HTML 목업 탐색은 사용자 결정으로 보류됨.

---

## 앱 구조 (4탭 IA)

```
홈 | 혜택 | 계산기 | 전체
```

상품 탭은 코드 보존 상태로 IndexedStack·하단 네비에서만 제외. 재활성화 시 `home_screen.dart`에 한 줄씩 되돌리면 됨.
금융상품 카드 기능은 **제외 확정(재제안 금지)**.

### 홈 (`home_screen.dart`)
- 상단 유형 탭(직장인/N잡러/프리랜서) — `_switchUserType` 즉시 전환. 유형별 가계부는 격리되지만 데이터는 안 사라짐(가져오기 배너)
- 회전 배너 — 카드별 X로 30일 숨김(`banner_states`). 절세팁 최대 2장
- 현황 패널: 이달 수령액·지출 → 가계부 링크. 예상연봉·지출목표는 유형별 독립 저장(`profile_type_values`)
  - **프리랜서**: "예상 연봉" 없음. 헤드라인 "이번 달 수입 (세후)" — 탭하면 세전 환산 페이드(저장 안 하고 표시 시점 역산)
  - **N잡러**: 근로소득·다른소득을 각각 serif 44로 대등 표시. **합계는 표시 안 함**(성격이 달라 오해 소지). 근로소득은 간이세액표라 세전 역산 불가 — 탭 전환 없음
  - 직장인 가계부의 올해 기타소득 소득금액이 300만원 초과 시 N잡러 전환 제안(연도별 1회)
- 카드공제 문턱 안내는 `_isEmployee` 분기(프리랜서는 카드 소득공제 대상 아님)
- 연중 가입자용 1~6월 소급 입력 배너 → `annual_backfill_screen.dart`
- 리마인더 패널, 세무도구 아코디언, 알림함 벨 배지

### 혜택 (`benefit_screen.dart`) — 9개 카테고리
정책·세무 혜택 카드. 전 항목 상세 + 공식 링크. 개인 상황 의존 항목은 전체-대상 알림에 넣지 않음.

### 계산기 (`calculator_screen.dart`) — 10개 카테고리
급여·근로 / 세금·연금·환급 / 주거·부동산 / 대출·저축 / 부동산 세금 / 청년·장병 / 출산·육아 / 의료·복지 / 일자리·행정 / 교통·에너지.
종부세·재산세는 외부 레퍼런스 대조 재구현: 재산세 **물건별 개별 누진 후 합산**(합산 일괄계산 시 과다),
공정시장가액 60% + 도시지역분 0.14% 옵션. 종부세는 1주택 12억/부부공동 18억/2주택+ 9억, 주택수 무관 단일 누진, 1세대1주택 고령·장기보유 공제 최대 80%.

혜택·계산기 탭은 우상단 돋보기 → **제목 유지한 채 아래로 검색창 펼침**(`AnimatedSize`). 제목을 가리면 안 됨.

### 전체 / 설정
전체: 기록(가계부·리마인더·알림설정) / 세금(세무도구·양식) / 내 계정(내 정보·설정).
설정: 알림·화면 테마(`theme_pref.dart`)·데이터 수집방식·백업/복원·보안(앱 잠금)·오류 기록 내보내기·개인정보처리방침·면책·파기·버전.

---

## 세무 계산 엔진 (`lib/core/tax_engine/`)

**기준 귀속연도: 2026** (`tax_year.dart`의 `TaxYear.reference`).
회귀 테스트 `flutter test` **168개** 전원 통과가 기준선.

- `tax_rates.dart` — 세율 8구간·인적공제·자녀/출산·금융소득·원천징수율·국민연금 상하한
- `employee_tax.dart` — 직장인 연말정산 전체 흐름. `calculateOtherIncomeAmount()`(기타소득 정률 40%)는 세 엔진 공유
- `freelancer_tax.dart` — 프리랜서 종소세(단순/기준경비율·실제 기장). 기타소득은 **별도 파라미터**로 받아 정률 40%만 과세(합쳐 넘기면 경비율 오적용). `compareBookkeepingVsEstimate()` — 간편장부 vs 추계 비교. 표준세액공제 7만(성실사업자 12만)
- `combined_tax.dart` — N잡러 합산(근로+사업+연금+기타)
- `insurance_engine.dart` — 4대보험. 요율은 전부 2026년 값(국민연금 4.75%/9.5%, 건보 7.19%, 장기요양 13.14%, 고용 0.9%)
- `reserve_estimator.dart` — 적립 카드용. `estimateForCurrentMonth({userType, allowanceCount})`가 저장 없이 매번 재계산
- `bookkeeping_duty.dart` — 기장의무 판정 + 무기장가산세
- `simple_ledger_builder.dart` — 가계부 → 국세청 간편장부 서식 + CSV(UTF-8 BOM)
- `occupation_data.dart` — 업종코드 1,542종 + 경비율

**기장의무 기준 (혼동 주의):**
- 직전연도 수입 3그룹: A(도소매·부동산매매·농림어·광업) **3억** / B(제조·숙박음식·건설·운수·정보통신·금융보험) **1.5억** / C(부동산임대·전문과학기술·교육·보건·예술·기타개인서비스 + 인적용역 940xxx) **7,500만**. 핵심 타깃 대다수가 C그룹
- 전문직은 수입 무관 무조건 복식부기
- **기장의무 그룹 임계 ≠ 경비율(단순/기준) 임계** — 다른 표
- **복식부기 자체 구현 안 함**(확정): 판정 시 계산 중단 + 세무사 권장 + 가계부 export 연결

---

## 세법 검증 체계 (2026-07-26 확립)

### 원칙 — 원문 없이 세법 상수를 고치지 않는다

이 세션에서 **2차 자료(검색 요약)로 코드를 고쳐 두 번 틀렸다.**
- 자녀 카드공제 한도를 "2025 개정"이라 적었으나 실제는 2026 개정
- 근로세액공제 감액을 조특령 §27⑨ `(1-감면급여비율)`로 구현했으나 실제 산식은 다름

둘 다 사용자가 "다시 확인해봐"라고 눌러서 잡혔다. 스스로 멈췄어야 했다.
**조문·국세청 책자 원문을 열기 전에는 코드를 건드리지 않는다.**

원문 보관: `지식_변환/원문텍스트/` (2026 개정세법 해설 전문, 월세공제 자료).
`pubspec.yaml` assets에 미등록 → **APK에 안 들어감**. 용량 걱정 불필요.

### 검토 방향 — 코드 → 법

종전 검토(법 → 코드: 개정 48항목을 읽으며 "앱에 영향 있나?" 판단)는 하나를 빠뜨려도 티가 안 난다.
실제로 조특법 §126의2 자녀 카드한도를 통째로 놓쳤다.

**엔진 상수를 전부 나열하고 각각에 "이 값의 적용 귀속연도는?"을 묻는다.** 다음 갱신도 이 방향으로.

### 값의 정확성은 테스트가, 적용연도는 문서가 지킨다

값이 틀리면 테스트가 깨진다. 하지만 **값은 맞고 연도만 틀린 오류는 테스트로 절대 안 잡힌다**
(틀린 전제로 테스트까지 쓰게 되므로). 연도 경계 항목만 `2026_개정세법_앱영향분석.md`에 기록.

### 페르소나 회귀 (34인)

`dbService`를 `InMemoryDatabaseHelper`로 갈아끼우고 실제 화면이 부르는 함수를 그대로 호출한다.
전원 수입·지출 각 3개월 이상(이번 달 포함). 코드만 읽어선 안 잡히는 배선 버그를 잡는다.
- `test/employee_persona_test.dart` — 직장인 12인
- `test/freelancer_persona_test.dart` — 프리랜서 10인
- `test/njob_employee_persona_test.dart` — N잡러 10인 + 직장인 8인

**렌더링 확인은 별도로 반드시 한다** — 테스트는 문구를 안 본다. 실제로 이번에도 렌더링에서만
잡힌 오류가 있었다(프리랜서 자녀 편집기가 카드공제를 안내).

### 2026-07-26 수정 내역 (확정 오류 12건)

| 유형 | 오류 | 방향 |
|---|---|---|
| 직장인 | 카드공제 자녀 한도 미반영 | 과대 계산 |
| 직장인 | 자녀 수 입력 UI 부재 → 자녀세액공제 항상 0 | 세금 과대 |
| 직장인 | 총급여 미파싱 시 의료비 3% 문턱 0 → 공제 과대 | 환급 과대 |
| 프리랜서 | **건보료 소득공제** — 지역가입자는 필요경비도 소득공제도 대상 아님 | 세금 과소 |
| 프리랜서 | **자녀세액공제 파라미터 부재** — §59의2는 종합소득자 전원 대상 | 세금 과대 |
| 프리랜서 | 성실사업자 표준세액공제 7만 고정(→12만) | 세금 과대 |
| N잡러 | **중소기업 감면이 부업 소득까지 감면** — 조특령 §27⑧ 안분 누락 | 세금 과소 |
| N잡러 | **감면+근로세액공제 이중 적용** — 국세청 산식 미적용 | 세금 과소 |
| N잡러 | 월세 17% 판정이 총급여만 봄(종합소득금액 4,500만 조건 누락) | 환급 과대 |
| 공통 | 무기장가산세를 결정세액 기준으로 계산(→산출세액) | 과소 |
| 공통 | **고향사랑기부금이 소득공제** — 실제는 세액공제 | 양방향 |
| 공통 | 기준 귀속연도 2025/2026 혼재 | — |

고향사랑 실측: 10만원 기부 시 15,000원 → **90,900원**(6배). 2천만원·45% 구간은 900만원 → 310만원(3배 과대).

---

## 세무도구 파이프라인

3화면: ①진단(`tax_simulator_screen.dart`) → ②가상신고서 → ③홈택스 가이드(`tax_annual_report_screen.dart`).
상단 `TaxPipelineRail(current:)` 공통. 업종코드 검색은 풀스크린 `OccupationSearchScreen`.

- **①진단**: 업종코드 → 기장의무 판정 → 수입 확정(PDF 파싱 or 직접 입력) → 경비(가계부 연동) → 기장 vs 추계 비교. 복식부기 판정 시 계산 숨김+세무사 권장. N잡러는 근로소득 PDF 가져오기 추가
- **②가상신고서**: ①결과 자동 채움(`report_drafts`)
- **③홈택스 가이드**: 직장인=연말정산 재계산 / 프리랜서·N잡러=국세청 전자신고 매뉴얼 기반 6단계 안내
- **장부 만들기**(`bookkeeping_guide_screen.dart`): 기장의무 판정 + 무기장가산세 20% 안내 + 간편장부 CSV export
- **메뉴 구성**(`tax_tools_screen.dart`): `taxRecordEntryFor()` — 프리랜서만 null(근로소득 없어 연말정산 무관). 경정청구는 직장인·N잡러만
- 업종코드 영속: `profile.occupation_code` 단일 출처

**남은 도메인 과제**: 74/82호 공식 서식 PDF 번들(국세청 수급 필요).

---

## 알림 시스템 — 리마인더 vs 알림 (분리 원칙)

- **리마인더**(개인): `kind='custom'` 자유 편집 / `kind='record'`(월급날+기록 넛지 18시) 시드 1회·시각만 편집. 매주 반복은 복수 요일. 면허·여권·전세만기는 프리셋 칩
- **이벤트형 기본 리마인더**(`event_reminder_prefs`, v28) 5종 — `budget_alert`, `inactivity_nudge`, `income_inactivity_nudge`, `recurring_expense_alert`, `tax_reserve_shortfall`(프리랜서·N잡러). 조건 해소 시 자동 취소
- **알림 히스토리 백필**(v27): OS 발화 알림을 앱 실행 시 체크포인트 이후 역산(최대 30일). 결제일·카드 알림은 아직 공백
- **알림**(전체 브로드캐스트): `system_reminder_catalog.dart` 정적 정의. notifId 대역 1001~1099 세금 / 1100 교통·에너지 / 1200 일자리·행정 / 2000 개인·월급·카드
- **원칙**: 개인 상황 의존 혜택·특정 연도 한정 이슈는 브로드캐스트 금지
- **미구현 갭 4종**: 프리랜서용 기록 넛지 문구, 5월 원천징수 정산 안내, 지급명세서 확인, 건보 지역가입자 전환 안내

---

## 가계부 (`expense_calendar_screen.dart` + `day_entry_screen.dart`)

- **레이아웃**: 달력 그리드 → 뷰 전환 탭(달력/분석/연간, 월간 네비 바로 아래) → 적립 예상 카드(프리랜서·N잡러) → 퀵설정 칩 행 + 범례
- **적립 카드**: `ReserveEstimator` 매번 재계산. min≠max면 "단순경비율(최소)~기준경비율(최대)" 근거 1줄 노출. 업종코드 미설정 시 숫자 숨김
- **일별 입력**: 날짜 탭/드래그 다중선택 → 풀스크린 `DayEntryScreen`. 수익 칩은 `LedgerProfile.of(userType).incomeTypes` 기준 — 직장인(급여/기타소득), N잡러(급여/사업소득/기타소득), 프리랜서(사업소득/기타소득). 원천징수 토글은 `tracksBusinessExpense` 유형만. 지출 3행 각각 "사업경비로 인정" 체크박스
- **유형 이동(가져오기)**: 다른 유형 기록이 있으면 상단 배너. 4방향만 — 직장인·프리랜서→N잡러(전 소득), N잡러→직장인·프리랜서(대상 지원 소득만, 나머지 잔류). `movableIncomeTypesTo(target)`가 급여→프리랜서·사업소득→직장인 유출을 원천 차단
- **퀵입력 프리셋**(v33), **핀치 줌**(1단계 7열 / 2단계 가로 2배+관성)

---

## DB 버전 이력 (현재 v39)

| 버전 | 변경 내용 |
|------|-----------|
| v1~30 | 기본 스키마 · 알림 · 정기결제 · 카드결제일 · 이벤트 리마인더 · 차량/주택 보유 · 유형별 예상연봉 |
| v31 | expense_items.is_business, income_entries.is_withheld, recurring_templates.is_business |
| v32 | occupation_code, property_value, 4대보험 가입여부 4종 |
| v33 | quick_entry_presets |
| v34 | prior_year_income, is_new_business, has_multiple_businesses (기장의무 판정) |
| v35 | expenses·income_entries에 user_type (가계부 유형별 분리. 기존 행은 NULL=공통. 조회는 `user_type = ? OR user_type IS NULL`) |
| v36 | error_log (최근 100건 순환) |
| v37 | monthly_income_records DROP (income_entries 직접 합산으로 이전 완료) |
| v38 | freelancer_health_insurance — **2026-07-26 기능 제거됨**(세법상 공제 대상 아님). 컬럼만 잔존, 읽는 코드 없음 |
| v39 | children_count_total (카드공제 자녀 한도용, 8세 미만 포함) |

**v34→v35 실기기 업그레이드 검증 미완** — 웹은 인메모리라 재현 불가.

---

## 핵심 데이터 모델

### ExpenseItem
```dart
id, date, endDate?,   // endDate 있으면 범위 지출
amount, content,
category,             // 지출 카테고리 (식비/교통/의류 등 10종)
paymentMethod,        // 신용카드 | 체크+현금 | 기타
isBusiness,           // (v31) 사업경비 인정 여부 — category와 독립 플래그
userType?             // (v35) NULL=공통(구 데이터), 값 있으면 해당 유형 전용
```

### IncomeEntry
```dart
id, date, amount, incomeType,  // '급여' | '기타소득' | '사업소득' | '기타'(레거시)
isWithheld,                    // true면 amount가 원천징수 후 순액 — 세전은 저장 안 하고
                               // 표시 시점 역산(사업 ÷0.967, 기타 ÷0.912)
userType?                      // (v35)
```
프리랜서 `isWithheld` 기본 true, 직장인·N잡러 기본 false.
근로소득('급여')은 간이세액표라 역산 불가 — 세전/세후 전환 UI 없음.

### RecurringTemplate / RecurringConfirmation (v25)
```dart
// Template: id, name, amountHint, category, paymentMethod, isBusiness, dayOfMonth(1~31), sortOrder
// Confirmation: (templateId, year, month) PK, status: 0=대기/1=확인/2=건너뜀, actualAmount, expenseId
```

---

## 기술 패턴 · 함정

### kIsWeb 가드
```dart
if (kIsWeb) { /* InMemoryDatabaseHelper — 새로고침마다 전체 리셋 */ }
else        { /* SqfliteDatabaseHelper */ }
```
저장 지속성·마이그레이션은 **실기기에서만 검증 가능**.
웹 프리뷰 확인 전에는 `flutter build web`을 반드시 재실행(APK만 빌드하면 프리뷰가 묵음).

### DB 마이그레이션
```dart
if (oldVersion < N) { try { await db.execute('ALTER TABLE ...'); } catch (e) {} }
// 항상 오름차순
```

### flutter_local_notifications v22
리시버 자동병합 안 됨 → `AndroidManifest.xml`에 `ScheduledNotificationReceiver`/`BootReceiver` 직접 선언 필수.

### 정확한 알람 권한 (Play 정책)
`USE_EXACT_ALARM`은 알람시계·캘린더 앱 전용 → `SCHEDULE_EXACT_ALARM`만 사용.
Android 13+는 사용자가 직접 켜야 하고, 안 켜면 `zonedSchedule`이 `exactAllowWhileIdle` 실패 시
`inexactAllowWhileIdle`로 자동 폴백.

### Flutter web `bottomNavigationBar` 렌더링 함정
`Row(children:[Expanded(...)])`를 높이 제약 없이 `bottomNavigationBar`에 직접 주면 body·appBar까지
통째로 안 그려짐. **동시에** 안드로이드에서는 `SafeArea`를 고정 높이 `SizedBox`로 감싸면
시스템 내비 인셋이 흡수돼 버튼이 가려짐. 정답 패턴:
```dart
bottomNavigationBar: SafeArea(top: false,          // 인셋은 바깥에서, 높이 제약 없음
  child: Container(padding: ...,
    child: SizedBox(height: 48,                    // 웹 렌더링용 높이는 안쪽 Row에만
      child: Row(children: [Expanded(...)]))))
```

### Flutter web은 캔버스 렌더
접근성 트리가 비어 브라우저 `read_page`/`form_input`이 안 먹는다 — 스크린샷 좌표로 조작해야 함.

### `tax_simulator_screen.dart` 중첩 함정
`build()`의 최상위 `if (_isFreelancer)`가 대부분을 감싼다. 새 카드를 그 안에 넣으면 직장인에게 안 보인다.

### 데이터 저장 보안
`sqflite_sqlcipher`로 DB 파일 자체 암호화. 키는 `flutter_secure_storage`(Android Keystore/iOS Keychain)에
기기 내부 생성·보관, 서버 전송 없음(`db_key_manager.dart`). 기존 평문 설치는 `initDatabase()`에서
1회 자동 이전(임시파일 검증 후 원자적 교체, 실패 시 원본 보존). 백업 JSON `formatVersion: 2`.
구버전 백업은 `legacy_migration.dart`로 자동 복호화.

---

## 남은 과제

### 최우선 — 실기기 검증 (사용자 수행)
코드는 완료됐으나 웹으로 재현 불가한 것들. **출시 전 반드시 확인.**
1. `9a69b86`(v34) 커밋으로 debug APK 빌드 → 실기기 설치 → 가계부에 급여 1건 기록
2. `main` 커밋으로 재빌드 → 같은 기기에 업그레이드 설치(데이터 보존)
3. 기록이 그대로 보이는지 확인(v35 마이그레이션이 기존 행을 NULL로 남기는지)
4. 유형 전환 후에도 "공통"으로 계속 보이는지
5. 앱 재실행 → 정상 구동되면 SQLCipher 자동 이전 완료
6. 생체 인증·PIN 폴백·백그라운드 복귀 잠금
7. 비행기 모드에서 폰트 3종 정상 렌더

### 엔진
- **경정청구 연도별 분기** (위 "가장 급한 것" 참조)
- **N잡러 환급 A/B/C 미지원** — `CombinedTaxCalculator`가 `actualExpense`를 안 받음. 세 유형 중 하나만 이 기능이 없음
- **표준세액공제 13만이 combined 경로에 없음** — 넣으려면 특별소득공제를 함께 포기하는 두 경로 비교가 필요. 현재 누락은 세금 과대(보수적) 방향이라 반쯤 고치는 것보다 나음
- 카드 추가공제 한도 구조 — 앱은 전통시장+대중교통+도서공연을 단일 300만 풀로 캡. 법은 전통시장·대중교통 200만 / 도서공연 포함 시 300만(총급여 7천 이하만). 현재 모든 호출부가 0을 넘겨 무효
- 프리랜서 성실사업자 요건을 저장하지 않아 월세공제 미적용(보수적)

### 출시
- Google 검토 승인 대기 → 테스터 12명 × 14일
- **최종 디자인 대수술** (아이콘 재제작 + IA·메뉴·기능배치 재설계) — 맨 마지막 단계
- 원격 분기·GitHub Pages 빌드 실패 미해결 (로컬 커밋 미push)
- iOS 실기기 검증 트랙

### 사용자 직접 작업
1. syncfusion_flutter_pdf Community License 조건(연매출 $1M 미만) 확인
2. 릴리스 keystore 생성 · `key.properties` 배치
3. 구 저장소(`vaekwoshi/connect`) 삭제
4. 경정청구용 「연말정산 신고안내」 2021~2024 귀속 4권 수급

---

## 사업 전략

자체성장 + 반매각 옵션. **B2B 성과수수료 전용**(앱 내 IAP 없음).
2모드 구조 — 1모드: 온디바이스 매칭 / 2모드: 리드 유출. **2모드 착수 전 중개업 법률 게이트 필수.**
무자본 마케팅: 결과공유카드 + 커뮤니티 시딩 + 예비창업패키지.
