# 세끌 (Sekkeul) — 프로젝트 컨텍스트

> 마지막 업데이트: 2026-07-13 (V1 출시 심사 완료 → 현재 상태로 전면 정리 + 출시 개발 계획 수립)

## 앱 개요

한국 직장인·프리랜서·N잡러를 위한 세금·가계부 관리 앱.
Flutter 3 / Dart 3, Android·iOS·Web 지원. **온디바이스 원칙**: 서버 없음, 모든 데이터는 기기에만 저장 (pubspec description에도 명시).

- 패키지명 `secul`, applicationId `com.sekkeul.app`, version 1.0.0+1
- targetSdk/compileSdk 36, 릴리스 서명은 `android/key.properties`에서 로드(없으면 debug 폴백 — 개발 계획 L-4 참고)
- 개인정보처리방침: `https://prismlayerd.github.io/sekkeul/` 호스팅 완료 (`PRIVACY.md`, `docs/index.html`, 문의 prismlayerd@gmail.com)
- 커스텀 스플래시 없음(실험 후 폐기) — `main.dart`가 `HomeScreen()` 직행. 런처 아이콘은 삼각형+글로우 디자인(`design/icon_final.png` → flutter_launcher_icons).

---

## 디자인 시스템: AppTheme v4 "Blueprint"

| 토큰 | 값 |
|------|-----|
| 배경 (라이트) | Concrete `#F8F7F5` |
| 배경 (다크) | Matte `#0D0D0D` |
| Accent | Blueprint Blue `#1F5AE0` |
| 서체 Display | DM Serif / Noto Serif KR |
| 서체 Body | DM Sans |
| 그림자 | **0** (없음) |
| Border-radius | ≤4px |

**하드 제약:** 바텀시트 금지 — fullscreen push / inline expand / AlertDialog만 허용.
`ElevatedButton` / `OutlinedButton` 금지 — 기본 CTA는 `GestureDetector + Container`.

### 타입 스케일 — `app_theme.dart` 상수

| 상수 | px | 용도 |
|------|----|------|
| `tsXS`    | 11 | 마이크로 라벨 / legend / eyebrow |
| `tsSM`    | 12 | caption / meta / 부차 정보 |
| `tsMD`    | 13 | compact UI / 탭 / 날짜 숫자 |
| `tsBase`  | 14 | 기본 본문 / 목록 항목 |
| `tsLG`    | 15 | 섹션 제목 / CTA |
| `tsXL`    | 17 | sans UI 헤더 |
| `serifSM` | 17 | 인라인 serif / 다이얼로그 헤더 |
| `serifMD` | 22 | AppBar 제목 (전 화면 통일) |
| `serifLG` | 28 | 섹션 헤딩 / 빈 상태 제목 |
| `serifXL` | 34 | 표시 숫자 |

특수 예외 (값 고정): `8.5` 달력 레인 초소형 / `18`, `22` 금액 입력 필드 / `44` 홈 히어로 / `10` 하단 네비.
**이 외 크기는 사용하지 않는다.**

※ 서체는 현재 `google_fonts` 런타임 페치 — 릴리스 매니페스트에 INTERNET 권한이 없어 실기기에서 폰트가 시스템 기본으로 폴백될 가능성 높음. **개발 계획 L-1(폰트 번들화)이 최우선.**

---

## 앱 구조 (5탭 IA)

```
홈 | 혜택 | 상품 | 계산기 | 전체
```

### 홈 (`home_screen.dart`) ✅ 완성
- 상단 유형 탭(직장인/N잡러/프리랜서) — 전환 시 확인 다이얼로그(`_confirmAndSwitchUserType`, 전용 기록 유무로 문구 분기).
- 회전 배너 — 카드별 X로 30일 숨김(`banner_states`). 절세팁은 최대 2장.
- 현황 패널: 이달 수령액·지출 → 가계부 링크. 예상연봉·지출목표는 유형별 독립 저장(`profile_type_values`). 가계부 복귀 시 `_goToLedger()`가 `_loadTypeValues` 재호출로 분석탭 편집분 동기화.
  - 프리랜서: "예상 연봉" 입력 없음(`_isEmployee` 게이트). 헤드라인 "이번 달 수입 (세후)" — 탭하면 세전 환산 페이드(`_showGrossIncome`, 세전은 저장 안 하고 표시 시점 역산).
  - N잡러: 근로소득(`_laborIncome`)과 다른소득(`_otherIncomeHeadline`)을 각각 serif 44 헤드라인으로 대등 표시. 합계는 어디에도 표시 안 함(성격이 달라 오해 소지). 근로소득은 간이세액표 기반이라 세전 역산 불가 — 탭 전환 없음.
  - 직장인이 기타수익 연 300만 초과 기록 시 N잡러 전환 제안 다이얼로그(`_checkNjobConversion`).
- 신용카드 공제 문턱 안내·지출목표 프롬프트 문구는 `_isEmployee` 분기(프리랜서는 카드 소득공제 대상 아님).
- 연중 가입자용 1~6월 소급 입력 배너 → `annual_backfill_screen.dart`(유형 무관 3종 소득 입력란 상시 노출, 이미 기록 있는 달 건너뜀).
- 리마인더 패널, 세무도구 패널(아코디언), 알림함 벨 배지 + 설정.

### 혜택 (`benefit_screen.dart`) ✅ 9개 카테고리 완성
정책·세무 혜택 카드 리스트. 전 항목 상세 설명 + 공식 링크. 개인 상황 의존 항목(기초연금 생일, 아동수당 등)은 전체-대상 알림에 넣지 않음(리마인더 후보로만).

### 상품 (`products_screen.dart`) ⏳ "서비스 준비 중" 플레이스홀더
금융상품 카드 기능은 제외 확정(재제안 금지). V1에서는 탭 숨김 처리 예정(개발 계획 L-5).

### 계산기 (`calculator_screen.dart`) ✅ 10개 카테고리 완성
급여·근로 / 세금·연금·환급 / 주거·부동산 / 대출·저축 / 부동산 세금 / 청년·장병 / 출산·육아 / 의료·복지 / 일자리·행정 / 교통·에너지 — 세부 항목은 화면 코드 참조.
종부세·재산세(`property_tax_screen.dart`)는 외부 레퍼런스 대조 재구현: 재산세 **물건별 개별 누진 후 합산**(합산 일괄계산 시 과다), 공정시장가액 60% + 도시지역분 0.14% 옵션. 종부세는 합산 기준(1주택 12억/부부공동 18억/2주택+ 9억), 주택수 무관 단일 누진, 1세대1주택 고령·장기보유 공제 최대 80%.

### 전체 (`all_screen.dart`) ✅ / 설정 (`settings_screen.dart`)
전체: 기록(가계부·리마인더·알림설정) / 세금(세무도구·양식) / 내 계정(내 정보·설정).
설정: 알림(마스터+세부)·데이터 수집방식·**백업/복원(`backupService.exportToFile` — 파일 내보내기/가져오기, 복원 시 덮어쓰기 확인)**·개인정보처리방침·면책·파기·버전.

---

## DB 버전 이력

| 버전 | 변경 내용 |
|------|-----------|
| v1~20 | 기본 스키마 |
| v21 | notification_log 테이블 |
| v22 | 알림 설정 |
| v23 | 알림함 (unreadCount) |
| v24 | payment_method 컬럼 마이그레이션 |
| v25 | recurring_templates + recurring_confirmations 테이블 추가 |
| v26 | card_payment_dates 테이블 추가 |
| v27 | app_state 테이블(알림 히스토리 체크포인트) + reminders.weekdays 컬럼 추가 |
| v28 | event_reminder_prefs 테이블 추가 (이벤트형 기본 리마인더의 on/off·시각) |
| v29 | user_profile에 owns_car/owns_house 컬럼 추가 (차량세·재산세/종부세 알림 대상 필터링용) |
| v30 | profile_type_values 테이블 추가 (user_type PRIMARY KEY, gross_income, expense_target — 유형별 예상연봉/지출목표 독립 저장) |
| v31 | expense_items.is_business, income_entries.is_withheld, recurring_templates.is_business 컬럼 추가 (프리랜서 사업경비 인정 여부 / 수입 세후·세전 여부) |
| v32 | user_profile에 occupation_code, property_value, pension_enrolled, health_enrolled, employment_enrolled, industrial_accident_enrolled 컬럼 추가 (프리랜서·N잡러 4대보험 가입여부 토글 + 업종코드 기반 경비율 추정용) |
| v33 | quick_entry_presets 테이블 추가 (즐겨찾기 퀵입력 프리셋 — 이름/금액/카테고리/결제수단 저장) |
| v34 | user_profile에 prior_year_income, is_new_business, has_multiple_businesses 컬럼 추가 (기장의무 판정용 — 직전연도 수입금액, 신규사업자/겸업 여부) |
| **v35** | expenses·income_entries에 user_type TEXT 컬럼 추가 (가계부 유형별 분리 — 기존 행은 NULL로 남아 모든 유형에서 공통으로 계속 보이고, 이후 새 기록부터 현재 유형으로 태깅. 조회는 `user_type = ? OR user_type IS NULL`) |

**v34→v35 실기기 업그레이드 검증 미완** — 웹은 인메모리라 재현 불가, 코드 리뷰로만 확인됨(개발 계획 L-6).

---

## 핵심 데이터 모델

### ExpenseItem
```dart
id, date, endDate?,   // endDate 있으면 범위 지출
amount, content,
category,             // 지출 카테고리 (식비/교통/의류 등 10종)
paymentMethod,        // 신용카드 | 체크+현금 | 기타
isBusiness,           // (v31) 사업경비 인정 여부 — category와 독립 플래그
userType?             // (v35) NULL=공통(구 데이터), 값 있으면 해당 유형 전용
```

### IncomeEntry
```dart
id, date, amount, incomeType,  // '급여' | '기타' (N잡러) | '사업소득' | '기타소득' (프리랜서)
isWithheld,                    // true면 amount가 원천징수 후 순액 — 세전은 저장 안 하고
                               // 표시 시점 역산(사업 ÷0.967, 기타 ÷0.912)
userType?                      // (v35)
```
프리랜서 `isWithheld` 기본 true, 직장인·N잡러 기본 false. 근로소득('급여')은 간이세액표라 역산 불가 — 세전/세후 전환 UI 없음.

### RecurringTemplate / RecurringConfirmation (v25)
```dart
// Template: id, name, amountHint, category, paymentMethod, isBusiness, dayOfMonth(1~31), sortOrder
// Confirmation: (templateId, year, month) PK, status: 0=대기/1=확인/2=건너뜀, actualAmount, expenseId
```

---

## 세무 계산 엔진 (`lib/core/tax_engine/`)

회귀 테스트 포함 `flutter test` 113개 전원 통과 유지가 기준선.

- `employee_tax.dart` — 직장인 연말정산 전체 흐름. `calculateOtherIncomeAmount()` = 기타소득 정률 40% 과세(필요경비 60% 자동인정)의 단일 구현 — 프리랜서·N잡러 엔진이 공유.
- `freelancer_tax.dart` — 프리랜서 종소세 (단순/기준경비율·실제 기장). `calculateTaxRange({accumulatedIncome, accumulatedOtherIncome, inputMonths, allowanceCount, occupationCode, ...})` — 사업소득은 업종 경비율, **기타소득은 별도 파라미터로 받아 정률 40%만 과세**(합쳐서 넘기면 경비율이 잘못 적용됨 — 2026-07-13 수정 완료). 노란우산공제 한도는 사업소득금액 단독 기준(법정 규칙). `compareBookkeepingVsEstimate()` — 간편장부(실제 경비) vs 추계(경비율) 중 유리한 쪽 자동 선택. 기장세액공제(20%·최대 100만)는 간편장부에 적용 안 됨(복식부기 자발 기장 전용) — 간편장부·추계 모두 표준세액공제 7만원.
- `combined_tax.dart` — N잡러 합산(근로+사업+연금+기타). 동일한 `calculateTaxRange` min/max.
- `insurance_engine.dart` — `calculateFreelancerInsurance({annualIncome, propertyValue, occupationCode})`. ⚠️ 지역가입자 건보 요율 0.0719는 공식 요율표 대조 미완(개발 계획 D-2).
- `reserve_estimator.dart` — 프리랜서·N잡러 가계부 적립 카드용. `estimateForCurrentMonth({userType, allowanceCount})`가 이번 달 기록+프로필로 `ReserveEstimate`(세금 min~max, 4대보험, 사용 가능 금액)를 저장 없이 매번 재계산.
- `bookkeeping_duty.dart` — 기장의무 판정: 전문직→복식(수입 무관) / 신규→첫해 간편 / 직전수입≥그룹임계→복식. 겸업만 `needsInputReview`.
- `occupation_data.dart` — 업종코드 테이블 + `bookkeepingGroup`(A/B/C, KSIC 6자리 코드 접두 판정)·`isProfessional` 게터.
- `tax_rates.dart` / `tax_year.dart` — 2025 귀속 세율 테이블.

**기장의무 기준 (검증 완료, 혼동 주의):**
- 직전연도 수입 3그룹: A(도소매·부동산매매·농림어·광업) **3억** / B(제조·숙박음식·건설·운수·정보통신·금융보험) **1.5억** / C(부동산임대·전문과학기술·교육·보건·예술·기타개인서비스 + 인적용역 프리랜서 940xxx) **7,500만**. 핵심 타깃 대다수가 C그룹.
- 전문직은 수입 무관 무조건 복식부기.
- 기장의무 그룹 임계 ≠ 경비율(단순/기준) 임계 — 다른 표.
- **복식부기 자체 구현 안 함**(확정): 판정 시 계산 중단 + 세무사 권장 + 가계부 export 연결. 복식부기 N잡러는 근로소득만 계산하고 caveat 명시.

---

## 세무도구 파이프라인 (구현 완료 상태)

3화면 파이프라인 ①진단(`tax_simulator_screen.dart`) → ②가상신고서 → ③홈택스 가이드(`tax_annual_report_screen.dart`). 상단 `TaxPipelineRail(current:)` 스텝 레일 공통. Blueprint 통일(바텀시트였던 업종코드 검색은 풀스크린 `OccupationSearchScreen`으로 전환).

- **①진단**: 업종코드 → 기장의무 판정(직전수입·신규·겸업 입력, v34 영속) → 수입 확정(원천징수영수증 PDF 파싱 or 직접 입력) → 경비(가계부 연동) → 기장 vs 추계 비교 → 유리한 방식 채택. 복식부기 판정 시 계산 숨김+세무사 권장. N잡러는 근로소득 PDF 가져오기(`_pickLaborWithholdingPdf`) 추가, 기납부 결정세액 반영.
- **②가상신고서**: ①결과 자동 채움(`report_drafts`), 하단 주 CTA로 ③ 연결.
- **③홈택스 가이드**: 직장인=연말정산 재계산 / 프리랜서·N잡러=`_buildBusinessBody()`가 report_drafts 항목 나열 + 국세청 전자신고 매뉴얼 기반 6단계 화면 흐름 안내. 진단 전이면 "①로 가기" 빈 상태.
- **메뉴 구성** (`tax_tools_screen.dart`): `taxRecordEntryFor()` — 프리랜서만 null(근로소득 없어 연말정산 무관), 직장인·N잡러는 "연말정산 기록하기" 노출. `taxPipelineFor()` N잡러 분기는 "빠진 공제 항목 찾기"가 첫 단계, 이후 합산 진단→가상신고서→홈택스. 경정청구는 `taxAmendedEntryFor`(직장인·N잡러만).
- 업종코드 영속: `profile.occupation_code` 단일 출처(My Info와 동일).

**남은 도메인 과제**: 74/82호 공식 서식 PDF 번들(원본 파일 없음 — 국세청에서 수급 필요, 다운로드는 사용자 동의 필요). N잡러 사업분 기장 vs 추계 비교는 명시적 스코프 제외.

---

## 알림 시스템 — 리마인더 vs 알림 (분리 원칙)

- **리마인더**(개인): `reminder.dart` + `custom_reminder_service.dart`. `kind='custom'` 자유 편집 / `kind='record'`(월급날+기록 넛지 18시) 시드 1회·시각만 편집. 매주 반복은 복수 요일(`weekdays`). 면허·여권·전세만기는 프리셋 칩으로 제목만 채운 커스텀 생성.
- **이벤트형 기본 리마인더**: `event_reminder_prefs`(v28)로 별도 관리, 5종 — `budget_alert`(예산 80%/초과, 16시 전 감지→당일 20시, 후→익일 9시), `inactivity_nudge`(지출 3일 미기록), `income_inactivity_nudge`(수입 미기록, 별도 토글), `recurring_expense_alert`(고정지출 결제일 매월 반복), `tax_reserve_shortfall`(적립 부족 경고, 프리랜서·N잡러). 조건 해소 시 자동 취소.
- **알림 히스토리 백필**(v27): OS 발화 알림이 알림함에 안 남던 공백을 앱 실행 시 체크포인트 이후 역산해 채움(최대 30일). 결제일·카드 알림은 아직 같은 공백 있음(후속).
- **알림**(전체 브로드캐스트): `system_reminder_catalog.dart` 코드 정적 정의, on/off는 `reminder_settings`. notifId 대역: 1001~1099 세금 / 1100 교통·에너지 / 1200 일자리·행정 / 2000 개인·월급·카드.
- **원칙**: 개인 상황 의존 혜택·특정 연도 한정 이슈는 브로드캐스트 금지.
- **미구현 갭 4종**: 프리랜서용 기록 넛지 문구, 5월 원천징수 정산 안내, 지급명세서 확인, 건보 지역가입자 전환 안내.

---

## 가계부 화면 (`expense_calendar_screen.dart` + `day_entry_screen.dart`)

- **레이아웃**: 달력 그리드 최상단 → 적립 예상 카드(프리랜서·N잡러, 이번 달만) → 퀵설정 칩 행(월급일/카드결제일/고정지출 — 한 줄 가로 스크롤) → 하단 뷰 탭(달력/분석/연간). 수입/지출/잔액 요약바는 분석탭과 중복이라 삭제됨.
- **적립 카드**: `ReserveEstimator` 매번 재계산, 최초 1회 안내 다이얼로그, 업종코드 미설정 시 "설정하면 더 정확해져요" 링크. 프로필(업종코드·재산액·4대보험)은 `my_info_screen.dart`에서 설정 — 유형 무관 단일 값(한 사람이므로 공유가 맞다는 결론).
- **일별 입력**: 날짜 탭/드래그 다중선택 → 풀스크린 `DayEntryScreen` push, 저장/삭제 후 pop되면 `_load()` 새로고침. 수익 입력은 급여(N잡러만)/사업소득(3.3%)/기타소득(8.8%, 60% 경비 자동인정) 칩 분리, 원천징수 토글 기본 true. 지출 3행(신용카드/체크·현금/기타) 각각 "사업경비로 인정" 체크박스.
- **퀵입력 프리셋**(v33): 자주 쓰는 지출 원탭 저장, 입력 화면 칩 행.
- **핀치 줌**: 1단계 기본 7열 / 2단계 가로 2배+스크롤+관성(`FrictionSimulation`).

---

## 기술 패턴 · 함정

### kIsWeb 가드
```dart
if (kIsWeb) { /* InMemoryDatabaseHelper — 새로고침마다 전체 리셋 */ }
else        { /* SqfliteDatabaseHelper */ }
```
저장 지속성·마이그레이션은 실기기에서만 검증 가능.

### DB 마이그레이션
```dart
if (oldVersion < N) { try { await db.execute('ALTER TABLE ...'); } catch (e) {} }
// 항상 오름차순
```

### flutter_local_notifications v22
리시버 자동병합 안 됨 → `AndroidManifest.xml`에 `ScheduledNotificationReceiver`/`BootReceiver` 직접 선언 필수(선언 완료).

### Flutter web `bottomNavigationBar` 렌더링 함정
`Row(children:[Expanded(...)])`를 높이 제약 없이 `bottomNavigationBar`에 직접 주면 body·appBar까지 통째로 안 그려짐. **동시에** 안드로이드에서는 `SafeArea`를 고정 높이 `SizedBox`로 감싸면 시스템 내비게이션 인셋이 흡수돼 버튼이 가려짐. 정답 패턴(두 요구를 다른 레이어에서 충족):
```dart
bottomNavigationBar: SafeArea(top: false,          // 인셋은 바깥에서, 높이 제약 없음
  child: Container(padding: ...,
    child: SizedBox(height: 48,                    // 웹 렌더링용 높이는 안쪽 Row에만
      child: Row(children: [Expanded(...)]))))
```
`Expanded` 없이 `Column(mainAxisSize: min)`만 쓰는 바(달력 하단 탭)는 `SafeArea`만으로 충분.

### 데이터 저장 보안 (현 상태)
`crypto_helper.dart`는 하드코딩 솔트 유도 키의 XOR+Base64 — **난독화이지 암호화가 아님**. expenses/income 금액·내용에 적용 중. "암호화" 표기 금지(개발 계획 S-1·S-2).

---

## 디자인 리디자인 탐색 (중단)

"Blueprint, Evolved" 방향으로 Figma(fileKey `rGZdjueEXrrG6CqOZPKii5`, rate limit로 중단)·HTML 목업(`design/home_redesign.html`, port 3001 design-preview) 두 경로 시도 후 사용자 결정으로 보류. 최종 디자인 대수술(아이콘 재제작+IA 재설계)은 맨 마지막 단계 원칙.

---

## V1 출시 심사 결과 (2026-07-13)

출시 심사위원회 리뷰 수행. 점수: 제품 74 / UX 70 / 기술 72 / **보안 52** / **출시 준비 58** → 🟡 일부 수정 후 출시(Android), iOS는 🔴 실기기 검증 전 보류.
**목표: 아래 개발 계획으로 전 항목 95+ 달성.** 감점 요소는 전부 아래 계획에 반영됨.

---

## 개발 계획 (실행: Sonnet 5)

원칙: 페이즈 순서대로. 각 항목은 완료 기준(✓)을 충족해야 종료. 모든 페이즈 공통 회귀 기준: `flutter analyze` 신규 이슈 0 + `flutter test` 113개 통과 + 웹 프리뷰 스모크. **APK 빌드는 사용자 명시 요청 시에만.**

### Phase L — 출시 차단 해소 (최우선)

- **L-1. 폰트 번들화** 〔출시·UX〕: DM Serif Display·Noto Serif KR·DM Sans를 `assets/fonts/`로 번들, pubspec `fonts:` 섹션 등록, `app_theme.dart`를 `fontFamily` 직접 지정으로 전환(google_fonts 의존 제거 또는 `allowRuntimeFetching=false`). ✓ 비행기 모드 실기기에서 serif/sans 정상 렌더 + 네트워크 요청 0 (폰트 파일 다운로드는 사용자 동의 후).
- **L-2. `allowBackup=false`** 〔보안·출시〕: AndroidManifest `android:allowBackup="false"` + `dataExtractionRules`(API 31+)·`fullBackupContent`(하위) 명시. 자체 백업/복원 기능이 있어 사용자 손해 없음. ✓ 매니페스트 반영 + 빌드 통과.
- **L-3. 면책 문구 삽입** 〔도메인·출시〕: 기존 `CalcDisclaimer`를 ①진단(`tax_simulator_screen.dart`) 결과 영역과 적립 카드(`expense_calendar_screen.dart` `_buildReserveCard` 펼침 하단)에 삽입. ✓ 두 화면 웹 프리뷰 확인.
- **L-4. 서명 fallback 안전장치** 〔출시〕: `build.gradle.kts` release 빌드에서 `key.properties` 부재 시 경고 로그 출력(또는 명시 플래그 없으면 실패). ✓ keystore 없이 release 빌드 시 경고/실패 확인.
- **L-5. 상품 탭 V1 숨김** 〔제품〕: 하단 네비를 4탭(홈|혜택|계산기|전체)으로. 상품 화면 코드는 보존(라우트만 제거). ✓ 4탭 렌더·기존 화면 이동 무결.
- **L-6. v34→v35 실기기 마이그레이션 테스트** 〔기능·출시〕: 구버전 APK 설치→데이터 생성→신버전 설치(업그레이드)→NULL 공통 데이터가 전 유형에서 보이는지 확인. *실기기 조작은 사용자 수행, 절차서와 검증 시나리오는 Sonnet이 준비.* ✓ 사용자 확인 완료 보고.
- **L-7. 프로필 소프트 게이트** 〔UX·제품·도메인〕: 가계부 화면 상단에 프로필(업종코드 등) 미작성 시 배너 — "프로필을 작성하면 적립 계산이 정확해져요" + `MyInfoScreen` 링크(하드 블록 아님, 그릴링 Q6 합의안). ✓ 미작성/작성 상태 분기 웹 프리뷰 확인.

### Phase S — 보안 95+ (온디바이스 원칙 유지)

- **S-1. 문구 정합(즉시)**: `PRIVACY.md`·`docs/index.html`·설정 화면·스토어 초안에서 "암호화" 표현 전수 조사 → "기기 내 저장(OS 앱 샌드박스 보호)"로 교체. Play 데이터 보안 양식 답변 가이드 문서화. ✓ "암호화" 잔존 grep 0건(또는 S-2 완료 후 사실이 된 표현만).
- **S-2. 실제 DB 암호화**: `sqflite` → `sqflite_sqlcipher` 전환 + 키는 `flutter_secure_storage`(Keystore/Keychain) 보관. **마이그레이션**: 최초 실행 시 평문 DB 열기 → XOR 필드 복호화 → 암호화 DB로 export → 평문 파일 삭제 → `CryptoHelper` 계층 제거(전 필드 평문 컬럼화, DB 파일 자체가 암호화). InMemory(웹) 경로는 무변경. ✓ 신규 설치·업그레이드 양쪽 마이그레이션 테스트 + 113개 테스트 통과 + 백업 파일 포맷 호환 유지(또는 버전 태그).
- **S-3. 앱 잠금**: `local_auth` 생체 인증 + 미지원 기기 PIN 폴백, 설정에서 on/off(기본 off), 앱 재개(resumed) 시 잠금 화면. Blueprint 스타일 준수(바텀시트 금지). ✓ on/off·생체 실패·PIN 폴백 시나리오.

### Phase T — 기술 구조 95+

- **T-1. 전역 에러 핸들링**: `runZonedGuarded` + `FlutterError.onError` → 로컬 `error_log` 테이블(DB v36, 최근 100건 순환) 기록. 설정에 "오류 기록 내보내기"(share_plus, 개인정보 미포함 스택만). 서드파티 크래시 SDK는 온디바이스 원칙상 도입 안 함. ✓ 강제 예외 발생 시 로그 적재·내보내기 동작.
- **T-2. `home_screen.dart` 분할**: 배너 캐러셀 / 현황 패널 / 리마인더 패널 / 세무도구 패널을 `lib/ui/screens/home/` 하위 위젯 파일로 추출(동작 무변경 리팩터링, 상태는 콜백/파라미터로). ✓ 파일당 ~500줄 이하, 픽셀 동일(웹 프리뷰 대조), 테스트 통과.
- **T-3. 화면 갱신 일원화**: `RouteObserver` 등록 + 복귀 갱신이 필요한 화면(홈, ③홈택스 등)에 `didPopNext` 리로드 — 산재한 "push 후 수동 리로드" 호출을 대체. ✓ 가계부→홈 지출목표 동기화 등 기존 시나리오 회귀 없음.
- **T-4. `monthly_income_records` 캐시 정리**: 유형 민감 소비처가 전부 우회 완료된 현 상태에서 잔여 소비처 조사 → 캐시 테이블·`_recalcMonthlyIncome` 제거(또는 유형 컬럼 추가 중 저비용 쪽). ✓ grep으로 참조 0 또는 유형 안전 확인.

### Phase U — UX 95+

- **U-1. 알림 권한 요청 시점 이동**: `main.dart`의 즉시 `requestPermissions()` 제거 → 첫 리마인더 생성/알림 설정 진입 시 맥락과 함께 요청. ✓ 첫 실행 무프롬프트, 리마인더 생성 시 요청.
- **U-2. 달력 상단 정리**: 월간 네비+뷰 전환 통합, 범례 간소화 등 보류했던 과밀 해소(사용자와 시안 합의 후 진행 — `/frontend-design` 활용). ✓ 사용자 승인.
- **U-3. 접근성 기본선**: 주요 인터랙션(유형 탭, 달력 날짜, 적립 카드, CTA)에 `Semantics` 라벨, 텍스트 배율 1.3에서 레이아웃 깨짐 점검·수정. ✓ TalkBack 주요 흐름 낭독 + 1.3배 무깨짐.

### Phase D — 도메인 검증 95+

- **D-1. 세율·요율 출처 표**: `tax_rates.dart`·`insurance_engine.dart`의 모든 상수에 출처·귀속연도 주석 + `tax_year.dart`에 연도 전환 체크리스트. ✓ 상수 전수 주석.
- **D-2. 건보 지역가입자 요율 0.0719 공식 대조**: 국민건강보험공단 고시와 대조, 다르면 수정 + 회귀 테스트 갱신. ✓ 출처 URL 주석 + 테스트.
- **D-3. 적립 카드 min~max 근거 노출**: 카드 펼침에 "단순경비율/기준경비율 두 가정" 1줄 설명(계산은 이미 정확 — 설명만 부재). ✓ 웹 프리뷰.

### 사용자 직접 작업 (코딩 외)
1. syncfusion_flutter_pdf Community License 조건(연매출 $1M 미만 등) 확인 — 미충족 시 Sonnet이 대체 라이브러리 검토.
2. Play Console: 개인정보처리방침 URL 입력, 데이터 보안 양식(S-1 가이드 기반), 정확 알람(`USE_EXACT_ALARM`) 핵심 기능 선언.
3. 릴리스 keystore 생성·`key.properties` 배치(L-4 이후).
4. L-6 실기기 업그레이드 테스트 수행, 최종 릴리스 서명 APK 비행기 모드 스모크.
5. iOS: 실기기 검증 트랙 별도 진행(그 전까지 iOS 제출 보류).
6. 구 저장소(`vaekwoshi/connect`) 삭제.

### 완료 판정
Phase L~D 전체 완료 + 사용자 작업 1~4 완료 시 재심사 → 전 항목 95+ 확인 후 Android 출시.
